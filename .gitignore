# dependencies
/node_modules

# next.js build output
/.next/
/out/

# env files — real keys live here, they must never reach a commit.
# .env.example is the template and is deliberately NOT ignored.
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# certificates jspdf's node build writes when a test forgets to stub save()
/little-fox-*.pdf

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# deploy
.vercel
